import SignUp from "./SignUp";

export const metadata = {
  title: "Sign Up Securly | Your Data, Your Control",
  description: "Create an acccount safely and manage your data with confidence",
};

const page = () => {
  return <SignUp />;
};

export default page;
